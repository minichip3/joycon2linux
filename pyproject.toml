[project]
name = "joycon2linux"
version = "0.1.0"
description = "Joy-Con 2 and Switch 2 Pro Controller bridge for Linux via BLE GATT"
requires-python = ">=3.10"
license = {text = "GPL-3.0"}
dependencies = [
    "bleak>=0.22.0",
    "evdev>=1.7.0",
    "click>=8.1.0",
    "pyyaml>=6.0",
]

[project.scripts]
j2l = "j2l.cli:cli"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
